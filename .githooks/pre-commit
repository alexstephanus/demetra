#!/usr/bin/env bash
set -e

cd "$(git rev-parse --show-toplevel)/firmware"

echo "Running cargo check-lib..."
cargo check-lib

echo "Running cargo lint..."
cargo lint

echo "Running cargo unit-test..."
cargo unit-test

echo "All pre-commit checks passed."
